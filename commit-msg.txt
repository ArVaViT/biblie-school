fix: critical quiz save, draft enrollment, chapter lock enforcement

- QuizEditor: create new quiz BEFORE deleting old one to prevent data
  loss if creation fails (was delete-first, risking total quiz loss)
- Block enrollment in unpublished/draft courses via API
- Enforce chapter lock check in self_complete_chapter so students cannot
  bypass sequential locks via direct API calls
- Add enrollment verification to self_uncomplete, teacher_complete, and
  teacher_uncomplete chapter endpoints
- Replace manual token extraction (_try_get_user) with proper FastAPI
  get_optional_user dependency (fixes test failures, cleaner DI)
- CourseDetail: enrollment failure now shows toast instead of replacing
  the entire page with an error screen
